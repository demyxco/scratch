#!/sbin/runscript

depend()
{
	need net
	use mysql dns netmount postgres
	after sshd
}

start()
{
	ebegin "Starting LiteSpeed Web Server"
	"/bin/lswsctrl" start 2>/dev/null 1>&2
	eend $?
}

stop()
{
	ebegin "Stopping LiteSped Web Server"
	"/bin/lswsctrl" delay-stop 2>/dev/null 1>&2
	eend $?
}

restart()
{
	ebegin "Restarting LiteSpeed Web Server"
	"/bin/lswsctrl" restart 2>/dev/null 1>&2
	eend $?
}
